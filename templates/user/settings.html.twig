{% extends 'base.html.twig' %}

{% block title %}Settings - Braindump{% endblock %}

{% block body %}
    <h1>Settings</h1>

    <div class="card">
        <h2 style="margin-top: 0;">Claude API Key</h2>
        <p>Enter your Anthropic API key to use Claude Code sessions. Your key is stored encrypted.</p>

        <form method="post">
            <div class="form-group">
                <label for="anthropic_api_key">Anthropic API Key</label>
                <input type="password" id="anthropic_api_key" name="anthropic_api_key"
                       placeholder="{% if hasApiKey %}Key is set (enter new value to replace){% else %}sk-ant-...{% endif %}">
            </div>
            {% if hasApiKey %}
                <p style="color: #2e7d32; font-size: 0.85rem;">An API key is currently configured. Submit an empty value to remove it.</p>
            {% endif %}
            <button type="submit" class="btn btn-primary">Save</button>
        </form>
    </div>
{% endblock %}
